* configuration for dumping overall champion to a file everytime a new champion is discovered
* try to get rid of deepcopy use
   * should be able to use contructors as copy
      * e.g. newList = list(oldList)
* more examples
* testing
* optimize
* gui
   - should be able to monitor/control parallel jobs through Linda
   - multiple lines indicating multiple connections
   - recurrant connection drawing
   - line width indicating weight
   - text weight on connection lines
   - open existing network file for display
   - load start genes
